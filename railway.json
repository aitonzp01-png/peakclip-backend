{
  "build": {
    "builder": "RAILPACK",
    "nixPkgs": ["ffmpeg", "python3", "deno"]
  },
  "deploy": {
    "startCommand": "uvicorn main:app --host 0.0.0.0 --port ${PORT:-8000}",
    "healthcheckPath": "/",
    "restartPolicyType": "ON_FAILURE"
  }
}